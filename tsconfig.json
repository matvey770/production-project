{
    "compilerOptions": {
      "outDir": "./dist/",
      // подсвечивает все места где не указан тип
      // однако это не означает, что теперь вовсе нельзя использовать any. это означает, что в подобных ситуациях разработчик дожен явно писать any
      "noImplicitAny": true,
      "module": "ESNext",
      //в какую спецификацию компилируем: для поддержки бол-ва браузеров es5
      "target": "es5",
      "jsx": "react-jsx",
      "allowJs": true,
      "moduleResolution": "node",
      // "baseUrl": ".",
      // esModuleInterop позволяет работать с пакетами, котоыре используют common js как с обаычными пакетами с помощью (import require() module.export) = common js
      // import Moment from 'moment';
      // без флага esmoduleinterop результат undefined
      // console.log(moment);
      // с флагом результат [object Object]
      // console.log(moment)
      "esModuleInterop": true,
      // если какая либо библиотека не имеет default import
      // лоадеры вроде ts loader или babel loader автоматически создают их
      // вместо такого импорта import * as react from 'react';
      // можно писать import react from 'react';
      "allowSyntheticDefaultImports": true
    },
    "ts-node": {
        "compilerOptions": {
            "module": "CommonJS"
        }
    }
  }